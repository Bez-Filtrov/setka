# 🕸️ Сетка VPN

> Практическое руководство по настройке V2Fly с TLSMirror для ускорения интернета

---

## 📋 Оглавление

1. [Введение](#introduction)
2. [Архитектура решения](#architecture)
3. [Установка](#installation)
4. [Подготовка домена](#domain-setup)
5. [Настройка сервера](#server-setup)
6. [Установка SSL-сертификатов](#ssl-certificates)
7. [Настройка клиента](#client-setup)
8. [Известные ограничения](#limitations)
9. [Заключение](#conclusion)

---

> **Примечание:** TLSMirror является экспериментальной функцией и может быть крайне нестабильным решением. Воспринимайте данное руководство только ради энтузиазма. По мере взросления инструмента данное руководство будет обновляться.

<h2 id="introduction">🔰 Введение</h2>

Ни для кого не секрет, что ТСПУ с каждым разом становится всё более изощрённее, но его главная проблема в том, что он гонится за популярными proxy-протоколами, и места под редко используемые инструменты в фильтрах у него не остаётся.

В рамках данного проекта мы рассмотрим детальную настройку инструмента, благодаря которому на свет появился всеми любимый Xray, но забытый после ряда разногласий между мейнтейнерами — **V2Fly**.

> **Примечание:** Вдаваться в подробности истории инструмента не будем, желающие могут найти информацию о нём самостоятельно.

### Почему V2Fly?

Главная суть в том, что V2Fly всё ещё развивается и внедряет новые фичи. На текущий момент последним «писком моды» является TLS-маскировка, которая всем известна по связке **VLESS + Reality** в Xray. V2Fly наконец реализовал собственное решение с похожим действием — **TLSMirror**.

Принцип работы аналогичен: «стянуть» TLS, зашифровать трафик и маскироваться под обычный HTTPS. Но с небольшим дополнением — генератор трафика. В отличие от Reality, который весь трафик завёртывает в прокси, TLSMirror генерирует обычный легальный трафик, в который «миксуется» прокси-трафик.

### Преимущества TLSMirror

| Особенность | Описание |
| ------------ | --------- |
| **TLS 1.2** | Возможность использовать TLS 1.2. Хоть РКН вроде как перестал блокировать TLS 1.3, но никто не знает, что будет завтра |
| **Необычный паттерн** | Для ТСПУ это необычный паттерн, его новизна обуславливает редкость использования |
| **Генерация трафика** | Смешивание прокси-трафика с легальным HTTPS-трафиком |

---

<h2 id="architecture">🏗️ Архитектура решения</h2>

Давайте изучим наш `docker-compose.yml`. Что мы видим:

### Компоненты системы

#### 1. 🌐 Mask (SSR-сайт)

> Собственный сайт под маскировку

Совсем «белый» HTML использовать как-то не комильфо, поэтому было решено написать простенький SSR-сайт, показывающий, какой покемон вы сегодня 😀

#### 2. 📜 acme.sh

acme.sh — это набор скриптов, которые занимаются выдачей SSL-сертификатов. Как certbot, только чуть более поворотливый.

#### 3. 🚪 NGINX

Прокси-сервер, который будет принимать входящие запросы от пользователей к нашему сайту.

Так как acme.sh в нашем варианте не будет поднимать собственный веб-сервер для подтверждения владения доменом, он будет переиспользовать NGINX с проброшенным 80-м портом. Также между ними мы будем использовать 2 хранилища:

- SSL-сертификаты
- Ключи acme.sh

#### 4. 📝 cert-init

Так как при холодном старте acme.sh ничего не знает про наш домен, а NGINX уже настроен на раздачу SSL-сертификата, нужно подложить временный самоподписной сертификат, который будет работать до тех пор, пока мы не выпустим реальный сертификат.

#### 5. ⚡ v2fly

Ядро нашей сборки — фреймворк для построения прокси.

---

<h2 id="installation">💾 Установка</h2>

Сначала вам необходимо клонировать данный репозиторий себе на сервер и на компьютер. Один репозиторий будет использоваться в качестве клиента и сервера.

V2Fly будет собираться из исходников, поэтому нам необходимо загрузить подмодули:

```sh
git submodule update --init --recursive
```

### Настройка переменных окружения

В каталог репозитория вам необходимо разместить файл `.env` на клиенте и на сервере со следующими данными:

```sh
UUID=$(uuidgen)
PRIMARY_KEY=$(cat /dev/urandom|head -c 32|base64)

MASK_DOMAIN=<ваш-арендованный-домен>
```

---

<h2 id="domain-setup">🌍 Настройка домена</h2>

После того как все компоненты будут запущены, необходимо настроить домен:

1. **Добавьте IPv4-адрес** в настройках DNS A-записи вашего домена
2. Он должен указывать на публичный IP-адрес сервера, на котором будет запускаться V2Fly

> ⚠️ **Важно:** Настройки DNS вступают в силу не сразу. Возможно, придётся подождать 5-10 минут.

---

<h2 id="server-setup">🖥️ Настройка сервера</h2>

Давайте запустим наш сервер:

```sh
docker-compose up -d
```

> ⏳ Что-то приходится компилировать из исходников, что-то перепаковывать. Придётся немного подождать, прежде чем всё соберётся.

---

<h2 id="ssl-certificates">🔒 SSL сертификаты</h2>

Теперь вам необходимо на сервере добавить в acme.sh наш новый домен и установить сертификат:

### Шаг 1: Выпуск сертификата

```sh
docker exec acme.sh --issue -d <ваш-домен> -w /var/www/acme --server letsencrypt -k 2048
```

✅ **Поздравляем!** Вы выпустили SSL сертификат 🥳

### Шаг 2: Установка сертификата

```sh
docker exec acme.sh --install-cert -d <ваш-домен> \
  --fullchain-file /var/www/certs/mask.crt \
  --key-file /var/www/certs/mask.key \
  --reloadcmd "docker exec nginx nginx -s reload"
```

Таким образом вы установите сертификат в NGINX и укажите автоматическую перезагрузку NGINX каждый раз при автоматическом перевыпуске нового сертификата.

✅ **Отлично!** Теперь на вашем домене должен открываться сайт с покемонами, хотя на самом деле весь трафик идёт через V2Fly.

---

<h2 id="client-setup">👤 Настройка клиента</h2>

Клиент для простоты будет запускаться из бинарника прямо в репозитории (возможно, потом тоже под Docker переделаю).

### Шаг 1: Сделайте скрипт исполняемым

```sh
chmod +x client/entrypoint.sh
```

### Шаг 2: Запустите клиент

```sh
cd client && ./entrypoint.sh
```

---

<h2 id="limitations">⚠️ Известные ограничения</h2>

### Отсутствие iOS-клиента

На наш взгляд, самый главный недостаток — отсутствие клиента под iOS. Под Windows есть, под macOS есть, под Android есть, а вот под iOS нет. Возможно, мы плохо искали, а список клиентов на сайте V2Fly давно не обновлялся. Если такой клиент есть, будем рады увидеть ваше issue.

### Проблема с РКН

На текущий момент большая часть дата-центров через границу режется на 16 КБ на одно TCP-соединение. Если вы разместите сборку на зарубежном сервере, то с большой вероятностью, особенно на мобильной сети, ТСПУ порежет вашу скорость.

**Выход из ситуации** (но он дорогой):

1. Разместить сборку на российском сервере в виде цепочки
2. До сервера РФ соединяться через Trojan + TLSMirror
3. Дальше трафик прокидывается до зарубежного сервера через более простой способ — Shadowsocks

> Российские сервера, как правило, имеют меньше фильтров ТСПУ из-за зоопарка в B2B сетях.

В будущем мы постараемся доделать сборку для охвата данного кейса.

### 📄 БС

Данное рукодство не имеет цели охватить кейс с БС. Есть достаточно информации о том, как они работают и какие там есть дыры. Адаптировать Reality сценарий под TLSMirror труда особого не составит, если понимаете, как это все работает.

---

<h2 id="conclusion">📝 Заключение</h2>

В данном репозитории мы отразили одну из своих игр с обузданием нового игрока на рынке TLS-маскировок.

> **Примечание:** Не факт, что это руководство будет обновляться со временем, хотя мы будем стараться эт делать. Поэтому данное руководство будет актуально на дату последнего коммита.

### Ложный сценарий с цепочкой серверов

Если рассматривать цепочку серверов с блокировками и т.д., то поддержка собственного зоопарка крайне затруднительна по труду и деньгам. Поднимать всё это советуем только при наличии энтузиазма и горящих глаз.

Мы же предоставляем для всех желающих собственный VPN сервис – «[**Без фильтров!**](https://t.me/bez_filtrov_vpn_bot)» построенный на базе Xray с учетнным большинством проблем и продолжаем ззаниматься его поддержкой по сей день (на дату последнего коммита) по стоимости дешевле чашки кофе ☕. Пользоваться нашим сервисом или поднимать собственное решение – решение уже за вами.

---

*Приступим!*
